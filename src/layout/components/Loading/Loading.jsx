import React from "react";
import { Loader, SwapLoading } from "./styled";

const Loading = () => {
  return (
    <SwapLoading>
      <Loader></Loader>
    </SwapLoading>
  );
};

export default Loading;
